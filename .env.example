DB_HOST=15.206.221.200
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=USER_GUIDE
PORT=3001
# Express JSON body limit for media uploads (base64 is ~33% larger than file size)
MAX_UPLOAD_BODY=200mb

# Public View API
PUBLIC_API_BASE_URL=http://localhost:3001
VIEW_TOKEN_SECRET=change-me-to-a-long-random-string
JWT_SECRET=change-me-to-a-long-random-string
# Comma-separated bootstrap read-only keys (optional)
VIEW_API_KEYS=

# Optional: protect /api/api-keys admin routes
ADMIN_API_SECRET=

# Shown on API Keys admin page (production URL for developer handoff)
VITE_PUBLIC_VIEW_API_URL=
